export enum State {
  Pending = 'pending',
  Finished = 'finished',
}

export enum ImportType {
  WORD = 'epub',
  PDF = 'pdf',
  WATTPAD = 'wattpad',
  EVERNOTE = 'evernote',
}

export enum ExportType {
  EPUB = 'epub',
  PDF = 'pdf',
}
